{
  "name": "salesview",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "SalesView — Dashboard interno de métricas de ventas (solo frontend, datos mockeados).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "gen:data": "node scripts/generate-data.js"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
