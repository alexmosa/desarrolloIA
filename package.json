{
  "name": "salesview",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "SalesView — Dashboard de métricas de ventas (frontend-only, datos mockeados).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate-data": "node scripts/generate-data.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
